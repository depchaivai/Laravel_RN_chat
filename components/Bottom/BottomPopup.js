import React from 'react'
import { ScrollView, StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    half: {
        height: 300,
        backgroundColor: 'tomato',
    }
})

const BottomPopup = () => {
    return (
        <ScrollView style={styles.half}>
            
        </ScrollView>
    )
}

export default BottomPopup
